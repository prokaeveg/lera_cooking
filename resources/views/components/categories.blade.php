<a href="{{ $url }}" class="category-row">
    <div class="category-item">
        <img class="category-img" src="{{ $image }}" alt="{{ $title }}" >
        <span class="category-title">{{ $title }}</span>
    </div>
</a>